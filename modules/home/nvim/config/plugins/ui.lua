-- Gitsigns setup
require('gitsigns').setup()

-- Lualine setup
require('lualine').setup({
  options = {
    theme = 'auto',
  },
})

-- Autopairs setup
require('nvim-autopairs').setup()

-- Comment setup
require('Comment').setup()
